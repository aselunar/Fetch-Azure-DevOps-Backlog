# Fetch Azure DevOps Backlog
A Python script to fetch and display ADO work items using the WIQL API.

Add your Azure DevOps organization URL, project name, and personal access token (PAT) in a `.env` file for secure access.
